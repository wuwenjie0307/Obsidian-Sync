---
date: "2026-05-29"
tags: [changelog, h20, crm, bot, port-mapping]
---

# h20 Bot 复用 48100 公网入口

## 改动类型

- [ ] 新功能
- [ ] Bug 修复
- [ ] 重构
- [x] 配置变更

## 最新结论

当前目标不是让生产服调用 h20 模型，而是让 h20 测试服完整跑通 Bot + VoxCPM + LatentSync，并让外部 CRM 测试服能访问 h20 Bot 的 `/crm/*` 接口。

在不新增公网端口映射的约束下，采用以下方案：

| 用途 | h20 内部地址 | 外部地址 |
|---|---|---|
| Bot 主服务 `/crm/*` | `http://127.0.0.1:8100` | `http://223.112.222.90:48100` |
| VoxCPM API | `http://127.0.0.1:8110` | 不作为外部联调入口 |
| LatentSync API | `http://127.0.0.1:8101` | `http://223.112.222.90:48101` 可保留 |

## 配置目标

h20 Bot 的 `config/config-dev.json` 只补丁式修改以下字段，不能整文件覆盖远程配置：

```json
{
  "server_port": 8100,
  "h20_api_base": "http://127.0.0.1:8110",
  "voxcpm_api_base": "http://127.0.0.1:8110",
  "latentsync_api_base": "http://127.0.0.1:8101"
}
```

## 验证入口

- h20 本机 Bot 健康检查：`curl -s http://127.0.0.1:8100/status/check`
- 外部 Bot 健康检查：`curl -s http://223.112.222.90:48100/status/check`
- 外部试听接口：`POST http://223.112.222.90:48100/crm/voice_clone_audition`

试听接口要求 `voice_file_url` 是 h20 能访问到的公网或内网可达音频 URL。

## 影响范围

- `48100` 从 VoxCPM 外部入口改为 Bot 外部入口。
- h20 内部 Bot 仍通过 localhost 调模型，不依赖生产服访问 h20 模型。
- 生产服模型方案不受影响。

## 相关文件

- `C:\Users\admin\Desktop\joyingbot-new\config\config-dev.json`
- `C:\Users\admin\Desktop\joyingbot-new\README.md`
- `C:\Users\admin\Desktop\joyingbot-new\docs\superpowers\plans\2026-05-29-h20-bot-48100-gateway.md`

## 2026-05-29 14:17 执行结果

已在 h20 完成端口切换：

- 远程配置备份：`/data/projects/joyingbot-new/config/config-dev.json.bak.20260529141706`
- Bot 主服务：`0.0.0.0:8100`
- VoxCPM API：`0.0.0.0:8110`
- LatentSync API：`0.0.0.0:8101`
- 远程配置已确认保留：`mysql_connection_ip=222.71.55.27`、`redis_host=222.71.55.27`

h20 本机验证通过：

- `curl -s http://127.0.0.1:8110/health` -> `{"status":"ok"}`
- `curl -s http://127.0.0.1:8101/health` -> `{"status":"ok"}`
- `curl -s http://127.0.0.1:8100/status/check` -> `{"status":"ok"}`
- `POST http://127.0.0.1:8100/crm/voice_clone_audition` 空 JSON -> HTTP 400，提示 `voice_file_url 不能为空`

从跳板/生产侧 `222.71.55.27` 验证公网入口通过：

- `curl -s http://223.112.222.90:48100/status/check` -> `{"status":"ok"}`
- `POST http://223.112.222.90:48100/crm/voice_clone_audition` 空 JSON -> HTTP 400，提示 `voice_file_url 不能为空`
- `curl -s http://223.112.222.90:48101/health` -> `{"status":"ok"}`

完整试听链路验证通过：

- 从跳板侧请求：`POST http://223.112.222.90:48100/crm/voice_clone_audition`
- h20 临时本机音频 URL：`http://127.0.0.1:9998/test_audio.wav`，仅用于测试，验证后已停止临时 HTTP 服务
- 返回：HTTP 200，`code=200`
- CDN 音频：`https://videos-test.joyingai.cn/video/crm/20260529/user4_1780035921505_5aba5419067e0d9b.wav`

注意：本地电脑直接访问 `223.112.222.90:48100` 仍超时，但跳板/生产侧访问通过。当前判断是公网入口存在来源网络限制；CRM/生产侧联调应以 `222.71.55.27` 或实际 CRM 测试服来源验证为准。
