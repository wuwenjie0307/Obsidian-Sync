---
tags: [task, model]
date: 2026-05-26
status: 全部部署完成
---

# 模型更换方案

## 目标

替换两个模型，部署到 h20 服务器本地：

| 替换什么 | 现用方案 | 替换为 | 状态 |
|----------|----------|--------|------|
| 唇形同步（数字人视频） | guiji2025/duix.avatar:2.9（Docker） | LatentSync（字节跳动） | ✅ 已部署 |
| 音色克隆 / TTS | 阿里云 DashScope cosyvoice-v3-plus（API） | VoxCPM（清华/面壁） | ✅ 已部署 |

---

## VoxCPM 部署记录 ✅

### 部署环境

| 项目 | 值 |
|------|-----|
| 服务器 | h20（223.112.222.90）/ hgx19 |
| Python | 3.13.5（系统 anaconda3，未用 conda 隔离） |
| CUDA | 12.4 |
| GPU | NVIDIA H20 96GB（第0块卡） |
| 版本 | voxcpm 2.0.3（VoxCPM2 模型） |
| 模型路径 | `/root/.cache/huggingface/hub/models--openbmb--VoxCPM2/` |
| 模型大小 | ~5 GB |
| 采样率 | 48kHz |

### 安装步骤

```bash
pip install voxcpm
# 首次运行自动下载 VoxCPM2 模型权重（~5GB）
```

### 踩坑记录

| 问题                              | 原因                                                | 解决                                                                               |
| ------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------- |
| GPU 不可用，回退到 CPU                 | pip 自动装的 PyTorch 2.12 需要 CUDA 12.8，但服务器驱动只支持 12.4 | 降级：`pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124` |
| torchaudio 报 libcudart.so.13 缺失 | torchaudio 2.11 与 torch 2.5 不兼容                   | 重装 torchaudio（pip 自动匹配版本）                                                        |
| 生成音频 4 秒                        | 正常，文本短对应音频短                                       | —                                                                                |

### CLI 使用

```bash
# 先连接 h20（从生产服务器跳板：sudo su → ssh root@h20 -p 10019）

# 基础合成
voxcpm --text "要合成的文本" --output /tmp/output.wav

# 语音克隆（需要参考音频）
voxcpm --text "要合成的内容" \
  --prompt-audio /path/to/voice.wav \
  --prompt-text "参考音频对应的文本" \
  --output /tmp/output.wav
```

### Python API

```python
from voxcpm import VoxCPM
model = VoxCPM.from_pretrained("openbmb/VoxCPM2")
wav = model.generate(text="你好世界", prompt_wav_path="speaker.wav", prompt_text=None)
```

---

## LatentSync 部署记录 ✅

### 部署环境

| 项目 | 值 |
|------|-----|
| 服务器 | h20（223.112.222.90）/ hgx19 |
| conda 环境 | `latentsync`（Python 3.10.13） |
| CUDA | 12.4 |
| GPU | NVIDIA H20 96GB（第0块卡） |
| 版本 | LatentSync 1.6 |
| 代码路径 | `/root/LatentSync/` |
| 模型路径 | `/root/LatentSync/checkpoints/` |
| 模型大小 | latentsync_unet.pt 4.8 GB + whisper/tiny.pt ~72 MB |
| 推理显存 | 18 GB（1.6版） |

### 安装步骤

```bash
# 1. 创建 conda 环境
conda create -n latentsync python=3.10.13 -y
conda activate latentsync

# 2. 安装 ffmpeg
conda install -y -c conda-forge ffmpeg

# 3. 安装系统依赖
sudo apt -y install libgl1

# 4. 安装 Python 依赖
cd /root/LatentSync
pip install -r requirements.txt

# 5. 下载模型权重（国内用镜像加速）
export HF_ENDPOINT=https://hf-mirror.com
huggingface-cli download ByteDance/LatentSync-1.6 whisper/tiny.pt --local-dir checkpoints
huggingface-cli download ByteDance/LatentSync-1.6 latentsync_unet.pt --local-dir checkpoints
```

### 关键依赖版本

| 包 | 版本 |
|------|------|
| torch | 2.5.1+cu121 |
| torchvision | 0.20.1+cu121 |
| diffusers | 0.32.2 |
| transformers | 4.48.0 |
| insightface | 0.7.3 |
| onnxruntime-gpu | 1.21.0 |
| gradio | 5.24.0 |

### 启动方式

```bash
# 1. 连接 h20（从生产服务器跳板）
ssh developer@222.71.55.27 -p 9527   # 先连生产服务器
sudo su                                # 切 root
ssh root@h20 -p 10019                  # 跳板到 h20

# 2. 激活环境
conda activate latentsync
cd /root/LatentSync
export HF_ENDPOINT=https://hf-mirror.com   # 国内加速

# 3. 命令行推理
python -m scripts.inference \
    --unet_config_path "configs/unet/stage2_512.yaml" \
    --inference_ckpt_path "checkpoints/latentsync_unet.pt" \
    --inference_steps 20 \
    --guidance_scale 1.5 \
    --enable_deepcache \
    --video_path "input.mp4" \
    --audio_path "input.wav" \
    --video_out_path "output.mp4"

# 4. Gradio Web 界面（调试用）
python gradio_app.py
```

### 踩坑记录

| 问题 | 原因 | 解决 |
|------|------|------|
| HuggingFace 下载极慢 | 国内直连慢 | `export HF_ENDPOINT=https://hf-mirror.com` 用镜像加速 |
| ONNX Runtime CUDA 加载失败 | 缺少 `libnvrtc.so.12` | InsightFace 人脸检测回退 CPU，不影响结果 |

### 测试结果

- Demo 视频推理成功，每步约 5.8 秒，16 步共 ~93 秒
- 输出：`video_out.mp4`

### API 服务测试（2026-05-28）

通过 HTTP API 调用测试，逻辑链路验证通过：

- **测试视频**: 17s mp4，576×1280
- **测试音频**: VoxCPM 生成的 1.76s wav（通过 reference_text 改善后效果清晰）
- **推理耗时**: ~86s（20 inference_steps）
- **输出**: 3.44s mp4，h264 576×1280 25fps + aac 16000Hz
- **效果**: 嘴型对齐正常，用户确认 OK

---

## 测试服务器 h20（223.112.222.90）/ 主机名 hgx19

| 项目   | 值                                            |
| ---- | -------------------------------------------- |
| 角色   | **完整测试服**，bot 全套服务本地运行（非纯 GPU 节点）            |
| IP   | 223.112.222.90:10019，无法直连，需从 222.71.55.27 跳板 |
| 连接方式 | 先切 root（`sudo su`），再 `ssh root@h20 -p 10019` |
| GPU  | 8 × NVIDIA H20（单张 96GB，总计 768GB）             |
| CUDA | 12.4                                         |
| 系统   | Ubuntu 22.04，内核 5.15                         |
| 磁盘   | 155GB 可用                                     |

### h20 全链路架构（测试服模式）

```
h20 本地
├─ bot 主服务（Flask + 调度 + 定时任务）
├─ VoxCPM TTS         → localhost:8100
├─ LatentSync 唇形    → localhost:8101
├─ Whisper 语音识别    → 本地进程池
├─ ffmpeg 视频处理     → 本地
├─ 文件上传           → 本地
├─ MySQL              → 222.71.55.27:3306（连生产库）
└─ Redis              → 222.71.55.27:6389（连生产库）
```

**所有模型调用都是 localhost，不存在跨服务器网络问题。** h20 只远程连生产服的 MySQL 和 Redis。

### 完整连接步骤

```bash
# 步骤1：VS Code SSH 连生产服务器（Host: joying-server, 222.71.55.27:9527）
# 步骤2：在生产服务器终端中
sudo su                  # 切 root（developer 用户没有 h20 的 SSH 权限）
ssh root@h20 -p 10019    # 跳板到 h20
```

### 网络拓扑

```
本地电脑 ──VS Code SSH──→ 222.71.55.27（生产） ──ssh跳板──→ h20（测试，需先sudo su切root）
```

### 已部署模型总览

| 模型 | 用途 | conda 环境 | Python | 代码路径 |
|------|------|-----------|--------|---------|
| VoxCPM | 音色克隆/TTS | 系统 anaconda3（base） | 3.13.5 | pip 全局安装 |
| LatentSync | 唇形同步/数字人视频 | `latentsync` | 3.10.13 | `/root/LatentSync/` |

## 当前生产服务器 222.71.55.27

| 项目 | 值 |
|------|-----|
| GPU | 2 × NVIDIA A800 80GB |
| CUDA | 12.2 |
| 状态 | 线上运行中，不宜部署测试 |
| supervisor 进程 | ai_autodone_py_prod / ai_autodone_py_test |
| 服务用户 | joying |

## 沟通记录

- **运维联系人**：张晋良
- **确认内容**：h20 机器用于部署新模型，8 张 H20 够用
- **部署方式**：裸机直跑，不用 Docker

## 现有代码相关文件

- TTS 服务：[router/service/video_server/tts_service.py](router/service/video_server/tts_service.py)
- 声音克隆：[router/service/video_server/qwen_audio_tts.py](router/service/video_server/qwen_audio_tts.py)
- 视频生成：[router/service/video_server/video_work.py](router/service/video_server/video_work.py)
- 新视频生成：[router/service/video_server2/video_work.py](router/service/video_server2/video_work.py)
- Whisper：[router/service/video_server/Whisper_model.py](router/service/video_server/Whisper_model.py)

---

## API 服务化（2026-05-28）

### 新增文件

| 文件 | 位置 | 用途 |
|------|------|------|
| `voxcpm_api.py` | `router/service/video_server/` | VoxCPM FastAPI 服务（端口 8100） |
| `latentsync_api.py` | `router/service/video_server/` | LatentSync FastAPI 服务（端口 8101） |
| `voxcpm_tts.py` | `router/service/video_server/` 和 `video_server2/` | VoxCPM HTTP 客户端 + 自动 fallback |
| `latentsync_service.py` | `router/service/video_server/` 和 `video_server2/` | LatentSync HTTP 客户端 + 自动 fallback |

所有服务放在 `router/service/` 下（遵循项目惯例，不再用 `api_server/`）。

### 切换机制

旧代码不删除，以注释形式保留。切换只需：
1. 注释/取消注释 import 行
2. 在 config.json 配置 API 地址

- **VoxCPM 客户端** (`voxcpm_tts.py`)：配置为空 → 自动走旧 DashScope
- **LatentSync 客户端** (`latentsync_service.py`)：配置为空 → 自动走旧 VideoGenService

### h20 服务状态

- **2026-05-28 18:42 检查**：两个 API 正常运行
  - VoxCPM API：端口 8100，进程 PID 2230214
  - LatentSync API：端口 8101，进程 PID 2239501（父进程）/ 2239513（子进程）
  - 健康检查均为 `{"status":"ok"}`
- 重启后需手动重新拉起（supervisor 待晋良哥配置）

### h20 conda 环境（晋良哥 2026-05-28 截图）

| 环境名 | 路径 | 用途 |
|--------|------|------|
| base | /data/server/anaconda3 | 系统默认（VoxCPM 装在这） |
| botserver | .../envs/botserver | — |
| joyingbot | .../envs/joyingbot | joyingbot-new 项目环境 |
| langchain | .../envs/langchain | — |
| latentsync | .../envs/latentsync | LatentSync 推理环境 |
| sglang | .../envs/sglang | — |
| swift | .../envs/swift | — |
| textgen | .../envs/textgen | 当前激活 |
| video | .../envs/video | — |
| wan_gpu | .../envs/wan_gpu | — |

latentsync 和 joyingbot 是我们建的，其余环境来源不明（多人共用机器）。

### 网络连通性进展

- **2026-05-28** 联系晋良哥开生产服→h20:8100/8101 防火墙，晋良哥回复需要找运营商反馈
- **2026-05-29** 晋良哥已开端口：
  - `223.112.222.90:48100` → VoxCPM API（内部 8100）
  - `223.112.222.90:48101` → LatentSync API（内部 8101）
  - ⚠️ bot 主服务 8018 端口尚未确认是否开放，CRM 联调需要此端口
- **git.joyingai.cn 内网 IP**：172.30.3.157（172.30 段），但 h20（公网 223.112.222.90）路由不到此内网 IP，ping 100% 丢包。h20 git 工作流仍用 SCP

---

## 待办

- [x] 部署 VoxCPM（音色克隆）
- [x] 部署 LatentSync（唇形同步）
- [x] 为 VoxCPM 搭建 HTTP API 服务 → `router/service/video_server/voxcpm_api.py`，端口 8100，已部署到 h20
- [x] bot 代码适配 → `/crm/voice_clone_audition` 端点已添加到 test_ai_autodone
- [x] 为 LatentSync 搭建 HTTP API 服务 → `router/service/video_server/latentsync_api.py`，端口 8101，已部署到 h20
- [x] bot 代码适配新模型（注释切换模式）→ voxcpm_tts.py / latentsync_service.py 已创建，video_work.py v1/v2 已加注释切换
- [x] h20 上 API 服务已手动启动（8100/8101 端口）并验证通过
- [x] VoxCPM 声音克隆测试通过（2026-05-28，带 reference_text 效果好）
- [x] 修复参数传递链路：reference_text + voice_emotion/speed/volume 从 CRM → video_work → voxcpm_tts → API
- [x] LatentSync 唇形同步 API 功能验证
- [x] 端到端测试 — test_e2e_video.py 串联 VoxCPM→LatentSync，67.5s 通过
- [x] h20 API 并发保护 — VoxCPM Semaphore(2) + LatentSync Semaphore(1)
- [x] 生产环境代码审查 — 确认 `/data/project/prod_ai_autodone/` 基线
- [x] git 提交推送 — `feature/ai_v1_api_merge`，MR #15 → master ✅（`d79a3e7a`）
- [x] DB 音色字段 — zhugedata_test.t_video_generate_task 新增 voice_emotion/speed/volume
- [x] video_work.py 正式切换到 VoxCPM + LatentSync（旧方案注释保留，可回退）
- [x] LatentSync Docker 化 — Dockerfile + docker-compose + deploy.sh + 运维文档，已推 joyingbot-new
- [x] ~~生产服→h20 网络连通~~ — **不需要**。生产部署后 VoxCPM/LatentSync 直接跑在生产服本地，所有调用都是 localhost，与 h20 无任何交互。h20 仅是开发测试环境。
- [x] 在 h20 部署完整 bot 测试服 — 启动全套 Flask 服务（调度+接口），本地调 localhost:8100/8101 ✅
- [x] 全链路真实任务验证 — CRM 提交一个任务 → h20 跑通全流程 ✅（2026-05-28 21:10，6/6 阶段全部通过，~80s）
- [x] 🔴 h20 入站端口白名单 — `223.112.222.90:48100/48101` 已开（2026-05-29 晋良哥），对应内部 VoxCPM :8100 和 LatentSync :8101。⚠️ bot :8018 待确认
- [ ] 多任务并发测试 — 同时提交 2 个任务，验证 Semaphore(VoxCPM 2/LatentSync 1) + GPU 不 OOM
- [ ] 音色参数测试 — 验证 voice_emotion(1-8)/speed(0.75-3.0)/volume(0-100) 各组合正常
- [ ] 真实 CRM 流程测试 — 从 CRM 测试服页面创建任务，走完整链路（非 curl）
- [ ] supervisor 配置（晋良哥）— 管理 bot 主服务 + voxcpm_api + latentsync_api 进程，照着生产服 supervisor 配置（`ai_autodone_py_test`）来就行。三个进程启动命令见 [生产环境架构](生产环境架构.md)
- [ ] Docker 打包 — LatentSync Docker 已写好，VoxCPM 可不打包（就一行 pip install）。生产部署时用 Docker 方案
- [ ] CRM 联调 — 代码就绪但未对接。阻塞原因：h20 入站端口被封，CRM 测试服无法访问。端口开后走完整 CRM 页面流程测试

---
 
## 代码仓库

| 项目 | 值 |
|------|-----|
| 仓库 | `https://git.joyingai.cn/services/crm.ai.joyingbot.git` |
| 分支 | `master`（已合并，`feature/ai_v1_api_merge` 已删除） |
| 最新 commit | `d79a3e7a` — MR #15 merged |
| 本地路径 | `c:\Users\admin\Desktop\joyingbot-new` |
| h20 路径 | `/data/projects/joyingbot-new/` |
| 同步方式 | h20 无法访问 git，通过 SCP tar.gz 覆盖；本地已 push 到远程 |

---

## CRM 接口参数映射

### 短视频任务列表返回的 voice 字段 → VoxCPM API

| CRM 字段 | VoxCPM API 参数 | 类型 | 可选值 |
|----------|----------------|------|--------|
| `voice_emotion` | `voice_emotion` | int (1-8) | 1=正常 / 2=亲切 / 3=热情 / 4=激昂 / 5=严肃 / 6=喜悦 / 7=悲伤 / 8=愤怒 |
| `voice_speed` | `voice_speed` | float | 0.75 / 1.0 / 1.25 / 1.5 / 2.0 / 3.0（默认 1.0） |
| `voice_volume` | `voice_volume` | int | 0-100（默认 50） |

### voice_emotion 枚举（CRM 传整数，API 同时支持 int/string）

| 值 | 中文 | API 可接受 |
|----|------|-----------|
| 1 | 正常 | `1` / `"1"` / `"normal"` |
| 2 | 亲切 | `2` / `"2"` / `"亲切"` |
| 3 | 热情 | `3` / `"3"` / `"热情"` |
| 4 | 激昂 | `4` / `"4"` / `"激昂"` |
| 5 | 严肃 | `5` / `"5"` / `"严肃"` |
| 6 | 喜悦 | `6` / `"6"` / `"喜悦"` |
| 7 | 悲伤 | `7` / `"7"` / `"悲伤"` |
| 8 | 愤怒 | `8` / `"8"` / `"愤怒"` |

### 接口路径

- **列表接口**: `POST /csm/agent/pc/video/generateTaskList`
- **提交接口**: `POST /submit_heygem_whisper_video_task`
- **试听接口**: `POST /crm/voice_clone_audition`（直接调 h20 VoxCPM API）

### voice_speed 注意事项

- CRM 正式规范**不允许 0.5**（仅 0.75~3.0），VoxCPM API 的 `ALLOWED_SPEEDS` 已对齐
- CRM 返回数据中曾出现过 `0.5`（可能是旧数据），代码以 CRM 正式规范为准

### 数据流

```
CRM任务数据(含voice字段) → submit路由(提取) → video_work(转发) → clone函数(传参) → API
```
✅ joyingbot-new 仓库已实现，旧仓库 joying-bot-server 未同步。

### 额外缺失字段：reference_text

- CRM 没有单独字段，但 video_work 中 `simplified_text`（Whisper 转录）可作为值
- 测试验证：传 `reference_text` 效果好，不传效果差

---

## 视频生成完整数据流

### 两阶段流程

```
┌─────────── CRM 提交任务 ───────────────────┐
│ voice_file_url       → 参考音色             │
│ original_creation_text → 要念的文案          │
│ original_video_url   → 数字人底板视频        │
│ voice_emotion/speed/volume → TTS 参数       │
└────────────────┬─────────────────────────────┘
                 ↓
┌─────────── 阶段1: 声音克隆 (VoxCPM) ───────┐
│ 服务: voxcpm_api.py :8100                   │
│ 输入: 参考音频URL + 文案 + emotion/speed/vol │
│ 输出: 合成音频 WAV (48kHz)                   │
│ 耗时: ~10s                                  │
└────────────────┬─────────────────────────────┘
                 ↓  合成音频 URL
┌─────────── 阶段2: 唇形同步 (LatentSync) ───┐
│ 服务: latentsync_api.py :8101               │
│ 输入: 数字人底板视频URL + 合成音频URL        │
│ 输出: 嘴型对齐最终视频 MP4                   │
│ 耗时: ~90s                                  │
└────────────────┬─────────────────────────────┘
                 ↓
           最终数字人视频
```

### 参数传递链路

```
crm_server.py submit 路由
  ├─ 提取 voice_emotion/speed/volume 字段
  ├─ 构建 payload
  └─ ↓
video_work_Heygem_Whisper()
  ├─ 接收 voice_emotion/speed/volume（默认值 normal/1.0/50）
  ├─ ↓
  ├─ clone_voice_and_synthesize(audio_url, text, task_id,
  │       reference_text=simplified_text,    ← Whisper 转录文本
  │       voice_emotion=voice_emotion,
  │       voice_speed=voice_speed,
  │       voice_volume=voice_volume)
  │   → VoxCPM API (8100) → 合成音频
  ├─ ↓
  └─ LatentSyncService.generate_video(audio_url, video_url, task_id)
      → LatentSync API (8101) → 唇形同步视频
```

### 当前配置

- **joyingbot-new (h20)**: video_work.py 已切换到 VoxCPM + LatentSync，旧方案注释保留
- **生产环境**: 待部署，需配置 `voxcpm_api_base` / `latentsync_api_base`
- voxcpm_api_base: 空（不配置）= 走旧 DashScope
- latentsync_api_base: 空（不配置）= 走旧 duix.avatar Docker
- 配置后自动切换，无需改代码

---

## LatentSync Docker 部署（2026-05-28 新增）

### 文件位置

`joyingbot-new` 仓库 `docker/latentsync/`：

| 文件 | 用途 |
|------|------|
| `Dockerfile` | 镜像定义（CUDA 12.1 + PyTorch 2.5.1 + LatentSync 1.6 + 模型权重） |
| `docker-compose.yml` | 单实例参考配置 |
| `deploy.sh` | 批量部署 8 实例脚本 |
| `README.md` | 完整运维文档（给晋良哥） |

### 关键参数

| 项 | 值 |
|------|-----|
| 镜像大小 | ~12 GB（含模型 5 GB） |
| 每容器显存 | ~18 GB |
| 8 容器合计 | 144 GB ≤ 160 GB（2×A800） |
| 每容器并发 | 1（Semaphore 串行） |
| 单次推理 | ~90s |
| API 端点 | `POST /v1/lip-sync`（同步返回 MP4） |
| 回滚方式 | config.json 设 `latentsync_api_base: ""` 即切回 duix.avatar |

### latentsync_api.py 改动

支持 Docker/裸机双模式：
- 有 conda → `conda run -n latentsync python -m scripts.inference`
- 无 conda → `sys.executable -m scripts.inference`
- 环境变量 `LATENTSYNC_DIR`、`LATENTSYNC_USE_CONDA` 可覆盖

### 仓库说明

注意：模型更换的新代码在 **joyingbot-new** 仓库（`git.joyingai.cn/services/crm.ai.joyingbot`），不在旧仓库 joying-bot-server。

| 仓库 | 路径 | 用途 |
|------|------|------|
| joyingbot-new | `c:\Users\admin\Desktop\joyingbot-new` | 新模型代码（VoxCPM + LatentSync），feature/ai_v1_api_merge 分支 |
| joying-bot-server | `c:\Users\admin\Desktop\joying-bot-server` | 旧代码（DashScope + duix.avatar），dev-lucky-yk-prod 分支 |

---

## h20 bot 全链路端到端测试（2026-05-28 晚上）

### 环境准备

| 步骤 | 状态 | 说明 |
|------|------|------|
| VoxCPM :8100 | ✅ | 正常运行 |
| LatentSync :8101 | ✅ | 正常运行 |
| Whisper 模型 | ✅ | openai-whisper 装完，下载 small 模型 |
| config-dev.json | ✅ | mysql→222.71.55.27, 加 voxcpm_api_base/latentsync_api_base |
| 代码同步 | ✅ | 通过 tar.gz 覆盖 10 个文件（git apply 失败改用覆盖） |
| 缺失依赖 | ✅ | openai-whisper, ffmpeg-python, opencv-python, punctuators |
| bot 启动 (8018) | ✅ | `python app_server_api.py --env dev --jobStatus false --port 8018` |

### 测试过程中修的 bug

1. **CRM 回调硬失败** — `safe_callback` 失败直接 `raise RuntimeError`，测试库无对应记录 → 改为 `logger.warning` 非致命
2. **VoxCPM 收到本地路径** — `tts_path` 是 `/tmp/...` 传给 VoxCPM API → 改为传上传后的 CDN URL `Original_audio_url`
3. **LatentSync onnxruntime CUDA 失败** — `libnvrtc.so.12` 不在 LD_LIBRARY_PATH → 待修复

### 测试结果

| 阶段 | 状态 | 耗时 | 说明 |
|------|------|------|------|
| Whisper 转录 | ✅ 通过 | ~2s | small 模型, cuda:0 |
| VoxCPM 音色克隆 | ✅ 通过 | ~2s | 正常合成, CDN 上传成功 |
| LatentSync 唇形同步 | ✅ 通过 | 73s | 见下方踩坑 |
| 字幕生成 (ASS) | ✅ 通过 | ~3s | Whisper word_timestamps + 繁简转换 + Punctuator 断句 |
| 字幕烧录 | ✅ 通过 | <1s | ffmpeg ass filter |
| 封面合成 | ✅ 通过 | ~1s | 图片转视频 + concat |
| **最终视频** | **✅** | **~80s** | `videos-test.joyingai.cn/...97eace0863788f41.mp4` (3.8s) |

### 第4-6阶段新增修的 bug

1. **LatentSync Face not detected** — 测试用的人脸视频某些帧 InsightFace buffalo_l 检测不到人脸 → 换 `/tmp/test_video.mp4`（之前 API 单独测试成功的视频）
2. **first_frame_judge_url 传了视频** — ffmpeg `-loop 1 -i video.mp4` 报 `Option not found`，封面合成阶段需要图片 URL 而非视频
3. **h20 时区冲突** — `/etc/timezone: Etc/UTC` vs `/etc/sysconfig/clock: Asia/Shanghai` → APScheduler 启动报 `ZoneInfoNotFoundError`，修复：`TZ=Asia/Shanghai` 环境变量
4. **8018 端口被占** — 旧 botserver 环境进程 (joying 用户, 8017) 残留，杀掉后重启

### h20 bot 启动日志关键信息

- MySQL 连接: `222.71.55.27:3306/zhugedata_test` ✅
- Redis 连接: `222.71.55.27:6389` ✅
- Whisper 模型: `small`, cuda:0
- 路由前缀: `/crm/` (Blueprint name = 'crm')
- 测试入口: `POST /crm/submit_heygem_whisper_video_task`
- 必填参数: original_video_url, original_audio_url, video_url, original_creation_text, first_frame_judge_url