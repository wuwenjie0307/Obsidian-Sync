---
date: 2026-05-27
tags: [daily]
---

# 2026-05-27 工作记录

## 已完成

### 新仓库部署（h20）
- 将 `git.joyingai.cn/services/crm.ai.joyingbot` clone 到 h20（hgx19）`/data/projects/joyingbot-new/`
- 创建 conda 环境 `joyingbot`（Python 3.10.13）
- 安装全部依赖，修复以下问题：
  - 移除 `pywin32`/`pypiwin32`（Windows 专属包）
  - 升级 `dashscope` 1.20.4 → >=1.25
  - 降级 `torch` 2.12.0 → 2.5.1+cu124（适配 CUDA 12.4）
  - 降级 `torchaudio` 2.11.0 → 2.5.1+cu124

### VoxCPM API 合并
- 将 h20 上的"野代码" `main.py`（8100 端口）合并进新仓库 `api_server/main.py`
- 创建 feature 分支 `feature/ai_v1_api_merge`
- 测试 API 8101 端口通过，`/health` 返回 200
- 推送代码到 git.joyingai.cn

### voice_clone_audition 端点迁移
- 将旧测试服的 `/crm/voice_clone_audition` 端点迁移到新仓库
- 添加 `h20_api_base` 配置项
- 添加 `from app_config.config import conf` 导入
- 提交并推送

### Obsidian 记录
- 写入 1 个 changelog + 5 个 bug 记录
- 更新项目概览和部署文档
- 标注 222.71.55.27 误部署为废弃

## 明天待办

- [ ] 在 222.71.55.27 删掉误部署目录 `/data/project/joyingbot-new`
- [ ] 找晋良：在 h20 配置 supervisor 管理 `api_server/main.py`（conda env: `joyingbot`，端口 8100）
- [ ] 找程伟/晋良：要 `git.joyingai.cn` 内网 IP，加到 h20 `/etc/hosts`
- [ ] 数据库新增音色字段（`voice_emotion`/`voice_speed`/`voice_volume`）
- [ ] 视频生成流程适配 VoxCPM 音色克隆
- [ ] LatentSync 唇形同步 HTTP API 服务搭建
- [ ] 给 CRM 团队提供 `voice_clone_audition` 对接信息

## 关键提醒

- h20 网络：不能直连 git.joyingai.cn，代码上传靠 SCP 跳板中转
- 生产服务器 222.71.55.27 不能随意操作端口/启动服务
- 代码分支规范：master → 个人分支 → test → MR → master
- VoxCPM API 在 h20 base 环境（8100）还在跑，新 joyingbot 环境（待 supervisor）会接管
