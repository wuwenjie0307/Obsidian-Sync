---
date: 2026-06-03
tags: [daily, h20, voice-clone, model-pool]
---

# 2026-06-03

## 今日工作汇总

- 梳理了 CRM 视频获客音色参数在 YApi、Bot 后端、h20 VoxCPM 服务之间的传参关系：确认 `voice_emotion`、`voice_speed`、`voice_volume`、`voice_file_url`、`personal_intro` 可以被接收，但文档示例仍有 `voice_speed=0.5` 与 `voice_emotion="normal"` 的不一致，需要统一到后端允许值。
- 处理音色克隆参数对效果的影响：确认默认情绪 + 默认语速 + 有准确 `reference_text` 时走 Hi-Fi 高保真克隆；只要用户调整情绪或语速，就切到 `reference_wav_path` 可控克隆；只调音量仍走 Hi-Fi + 后处理。
- 修正了 `(温柔甜美，亲切自然，语速稍快)` 这类风格提示被 VoxCPM 当作正文读出来的问题：Hi-Fi 模式不再拼 style prompt；可控克隆模式才把风格提示用于模型控制。
- 同步了完整视频生成链路的音色克隆逻辑：h20 视频模型池 VoxCPM `8120 / 8122 / 8124 / 8126` 已重启并检查 `/health`，完整视频任务会使用新的 Hi-Fi / 可控克隆切换策略。
- 处理试听接口并发问题：先验证过 Redis slot 方案可以保护长请求，但用户明确指出不能占用视频生成模型池后，已修正为“试听专用 VoxCPM Docker + `t_comfyui_config.config_key='voice_audition_url'` 试听池”的方案。
- 本地已完成试听池代码雏形：`/crm/voice_clone_audition` 改为领取 `voice_audition_url` lease，只调用 `config_value_audio` 的 VoxCPM base；抢不到时返回 `503 试听服务繁忙，请稍后重试`，不会再落到 `comfyui_url` 视频池。
- 本地验证通过：`test_voice_audition_pool_service`、`test_voice_clone_upload`、`test_scheduled_video_voice_params` 共 28 tests OK；相关 Python 文件 py_compile OK；`git diff --check` OK，仅有 LF/CRLF 警告。
- 已把“独立 Docker + t_comfyui_config 试听池”的纠正方案记录到项目笔记：[[projects/joying-bot-server/docs/h20-voice-clone-params-prompt-optimization-2026-06-03|h20 音色克隆参数与试听并发记录]]。

## 今日未完成，已转明日

- [ ] 提交并同步新的“试听专用 VoxCPM Docker + `voice_audition_url` 试听池”实现到 `test` 分支，覆盖/替代今天早些时候的 Redis slot 试听限流方案。
- [ ] 确认当前工作区里 `scheduler/collect_scheduler.py` 和 `test/test_scheduled_video_voice_params.py` 的未提交改动归属，决定和试听池改动一起提交还是拆分提交。
- [ ] 确认 h20 上新试听 VoxCPM Docker 的 GPU 分配和端口 `8128` 占用情况，避免和视频生成模型池抢资源。
- [ ] 在 h20 现场 compose 中只追加 `voxcpm-audition-api-1`，不能覆盖现场已有四组视频生成模型池配置。
- [ ] 启动 `8128` 试听 VoxCPM 服务并检查 `/health`。
- [ ] 向 `zhugedata_test.t_comfyui_config` 插入 `config_key='voice_audition_url'` 的试听池行。
- [ ] 部署/重启 Bot 入口，使 `8100 / 48100` 读取新代码。
- [ ] 实机验证 `/crm/voice_clone_audition`：正常试听走 `8128`，锁定/释放 `voice_audition_url`，并确认 `comfyui_url` 视频池行完全不被占用。
- [ ] 清理或标记项目笔记里早期 Redis slot 方案为“已废弃/被纠正”，避免后续误读。
- [ ] 同步修正 YApi：`voice_emotion` 示例统一为数字，`voice_speed` 示例去掉 `0.5`，`generateTaskList` 响应补齐音色字段。
