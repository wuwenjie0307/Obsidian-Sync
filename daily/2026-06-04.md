---
date: 2026-06-04
tags: [daily, todo, h20, voice-clone, model-pool]
---

# 2026-06-04

## 今日待办

- [x] 提交并同步新的“试听专用 VoxCPM Docker + `voice_audition_url` 试听池”实现到 `test` 分支，确保 test 分支不再停留在 Redis slot 试听限流方案。
- [x] 检查当前工作区未提交改动归属：试听池相关文件、`scheduler/collect_scheduler.py`、`test/test_scheduled_video_voice_params.py` 是否需要拆分提交。
- [x] h20 部署前确认：GPU 分配、端口 `8128` 是否空闲、现场 compose 当前四组视频池服务是否完整。
- [x] 在 h20 现场 compose 只追加 `voxcpm-audition-api-1`，保留四组 `8120/8121`、`8122/8123`、`8124/8125`、`8126/8127` 视频池配置。
- [x] 启动 `voxcpm-audition-api-1`，验证 `http://127.0.0.1:8128/health` 正常。
- [x] 插入测试库试听池配置行：`config_key='voice_audition_url'`、`config_value_audio='http://127.0.0.1:8128'`、`type=2`、`is_active=1`。
- [x] 部署/重启 Bot 入口，让 `8100 / 48100` 使用新的试听池代码。
- [x] 实机调用 `/crm/voice_clone_audition` 验证正常试听、繁忙返回、lease 释放；同时查询 `t_comfyui_config` 确认只锁 `voice_audition_url`，不锁 `comfyui_url`。
- [ ] 复测完整视频生成链路，确认视频任务仍使用四组 `comfyui_url` 视频池，试听和视频生成资源不互抢。
- [x] 更新项目笔记：把早期 Redis slot 方案标为废弃/被新方案替代，保留最终口径。
- [x] 修正 YApi 文档不一致：`voice_emotion` 统一数字值，`voice_speed` 统一到后端允许值，`generateTaskList` 响应补齐音色字段。
- [ ] 视实机结果决定是否继续扩展多个试听 VoxCPM 实例，或后续改为异步试听任务。

## 上午进展

- [x] 试听池实现已提交：`fdfda7be feat: add dedicated voice audition pool`。
- [x] 已合入并推送远端 `origin/test`：`0cddb187 Merge branch 'h20-model-pool-productionize' into test`。
- [x] 本地 feature worktree 验证通过：`python -m unittest test.test_voice_audition_pool_service test.test_voice_clone_upload test.test_scheduled_video_voice_params`，30 tests OK；`py_compile` OK；`git diff --check` OK，仅 CRLF warning。
- [x] test worktree 合并后验证通过：29 tests OK；`py_compile router/service/voice_audition_pool_service.py router/crm_server.py` OK。
- [x] YApi/apidoc 已核对：707、702、1936、700 当前已是数字 `voice_emotion`、允许值内 `voice_speed=0.75`，且 `generateTaskList` 响应已包含音色字段。
- [x] h20 实机部署已完成：8128 试听 VoxCPM 已启动，DB 行已插入，8100 已重启到当前 Jenkins 目录；外部 `48100` 从当前网络仍 HTTP 000/TCP 超时。

## h20 实机落地结果

- [x] `origin/test` 已推送到 `0cddb187`，Jenkins 已部署到 `/data/project/test_ai_botserver.20260604103337`。
- [x] h20 当前 `8017` 和 `8100` 都运行在 `/data/project/test_ai_botserver.20260604103337`，`/status/check` 均返回 `{"status":"ok"}`。
- [x] 试听专用 Docker 已启动：`voxcpm-audition-api-h20-test-1`，端口 `8128`，GPU `7`，`/health` 返回 `{"status":"ok"}`。
- [x] 现场 compose 已备份：`/data/project/test_ai_botserver/deploy/docker/docker-compose.h20.yml.bak.20260604101303`，只追加了 `voxcpm-audition-api-1` 服务。
- [x] 测试库已插入 `t_comfyui_config` 行：`id=12`、`config_key=voice_audition_url`、`config_value_audio=http://127.0.0.1:8128`、`is_active=1`、`type=2`。
- [x] 正常试听实测通过：`POST /crm/voice_clone_audition` 返回 HTTP 200 / `code=200`，生成 CDN wav。
- [x] 繁忙分支实测通过：临时锁定 `voice_audition_url` 后接口返回 HTTP 503 / `试听服务繁忙，请稍后重试`，随后已恢复 `is_active=1`。
- [x] DB 复查确认：四组 `comfyui_url` 视频池行仍保持原状态，试听没有锁视频生成池。
- [ ] 当前网络访问外部 `http://223.112.222.90:48100/status/check` 仍为 HTTP 000/TCP 超时；h20 内网 8100 验证正常。
- [ ] 安全清理待用户执行：我这边无权限清除 Machine 级 `H20_JUMP_PASSWORD`，需要管理员 PowerShell 手动清理。



### h20 音色克隆悲伤/愤怒提示词试听验证
- 已记录完整 changelog：[[projects/joying-bot-server/changelog/h20-voice-emotion-prompts-test-2026-06-04|h20 音色克隆悲伤/愤怒提示词更新与试听验证]]。
- h20 测试服已更新悲伤/愤怒新版提示词，并将 `STYLE_PROMPT_MAX_CHARS` 调到 `180`。
- 试听结果：悲伤 `10.080s`，愤怒 `8.480s`，均 HTTP 200 / `code=200`，日志确认新版 style prompt 进入 VoxCPM 模型输入。
- 已验证 `8100`、`8128-8131` 健康；视频池 `8120/8122/8124/8126` 和遗留 `8110` 也已同步过新版提示词。

### h20 长文案音色情绪默认语速提示修复
- 已记录完整 changelog：[[projects/joying-bot-server/changelog/h20-voice-long-text-default-speed-prompt-2026-06-04|h20 长文案音色情绪默认语速提示修复]]。
- `origin/test` 当前已包含 `1f030117 fix: omit default VoxCPM speed prompt`；另推了干净个人分支 `origin/lucky-test/voxcpm-default-speed-prompt-clean` 作为只含本轮修复的参考分支。
- h20 已重启 VoxCPM 视频池 `8120/8122/8124/8126` 和试听池 `8128/8129/8130/8131`，health 全部 OK。
- 直接 8128 烟测：短文案 `38` 字返回 200 / `6.240s`，长文案 `312` 字返回 200 / `48.320s`。
- Bot 8100 试听接口烟测：短文案返回 CDN `user4_1780569361365_b3a67d6c4e5b2b1b.wav`，长文案返回 CDN `user4_1780569382402_0fe0b26c82753869.wav`。
- 日志关键证据：`voice_speed=1.0` 时短/长分别为 `raw_text_len=38 -> styled_text_len=63`、`raw_text_len=312 -> styled_text_len=337`，`style_prefix_len=25`，说明没有再追加 `自然语速`。
- 待办：产品主观试听两条 CDN；如果长文案仍平淡，再进入 P1 分段生成方案。

### h20 亲切/热情/激昂/严肃/喜悦提示词上线验证
- 已记录完整 changelog：[[projects/joying-bot-server/changelog/h20-voice-emotion-prompts-2-6-2026-06-04|h20 亲切/热情/激昂/严肃/喜悦提示词上线验证]]。
- `origin/test` 已更新到 `7dae3c4f`，h20 Jenkins 当前软链为 `/data/project/test_ai_botserver.20260604185023`。
- 已重启 VoxCPM 视频池 `8120/8122/8124/8126` 和试听池 `8128/8129/8130/8131`，live code 检查确认 `new_2` 到 `new_6` 均为 `True`，悲伤/愤怒仍为 `True`。
- 8 个 VoxCPM 容器 health 全部 OK。
- 8128 试听池烟测：`voice_emotion=2` 返回 HTTP 200 / `audio/wav` / `307244` bytes / `1.750s`；日志确认 `raw_text_len=24 -> styled_text_len=87`，`style_prefix_len=63`。
