---
date: 2026-06-04
tags: [daily, todo, h20, voice-clone, model-pool]
---

# 2026-06-04

## 明日待办

- [ ] 提交并同步新的“试听专用 VoxCPM Docker + `voice_audition_url` 试听池”实现到 `test` 分支，确保 test 分支不再停留在 Redis slot 试听限流方案。
- [ ] 检查当前工作区未提交改动归属：试听池相关文件、`scheduler/collect_scheduler.py`、`test/test_scheduled_video_voice_params.py` 是否需要拆分提交。
- [ ] h20 部署前确认：GPU 分配、端口 `8128` 是否空闲、现场 compose 当前四组视频池服务是否完整。
- [ ] 在 h20 现场 compose 只追加 `voxcpm-audition-api-1`，保留四组 `8120/8121`、`8122/8123`、`8124/8125`、`8126/8127` 视频池配置。
- [ ] 启动 `voxcpm-audition-api-1`，验证 `http://127.0.0.1:8128/health` 正常。
- [ ] 插入测试库试听池配置行：`config_key='voice_audition_url'`、`config_value_audio='http://127.0.0.1:8128'`、`type=2`、`is_active=1`。
- [ ] 部署/重启 Bot 入口，让 `8100 / 48100` 使用新的试听池代码。
- [ ] 实机调用 `/crm/voice_clone_audition` 验证正常试听、繁忙返回、lease 释放；同时查询 `t_comfyui_config` 确认只锁 `voice_audition_url`，不锁 `comfyui_url`。
- [ ] 复测完整视频生成链路，确认视频任务仍使用四组 `comfyui_url` 视频池，试听和视频生成资源不互抢。
- [ ] 更新项目笔记：把早期 Redis slot 方案标为废弃/被新方案替代，保留最终口径。
- [ ] 修正 YApi 文档不一致：`voice_emotion` 统一数字值，`voice_speed` 统一到后端允许值，`generateTaskList` 响应补齐音色字段。
- [ ] 视实机结果决定是否继续扩展多个试听 VoxCPM 实例，或后续改为异步试听任务。
